{
  "scripts": {
    "start": "http-server -p 8000 ./src/",
    "cypress:open": "cypress open",
    "test:local": "cypress run",
    "test": "printenv && cypress run && visualcop-cli ./cypress/screenshots/**"
  },
  "devDependencies": {
    "cypress": "^2.1.0",
    "http-server": "^0.11.1",
    "visualcop-cli": "^0.0.4"
  }
}
